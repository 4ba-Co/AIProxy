branch:
  - reg: "^main"
    buttons:
      - name: AIProxy.Proxy Build
        description: 构建 AIProxy.Proxy 镜像
        event: web_trigger_proxy_build
        permissions:
          roles:
            - owner
        inputs:
          var1:
            name: 版本
            description: 构建版本
            required: true
            type: input
            default: 1.0.0